{
  "name": "claude-compact-openrouter",
  "version": "0.7.0",
  "description": "Verbatim Jev-guided compaction for Claude Code sessions, with Jev served through OpenRouter or TypeSafe's own API.",
  "license": "MIT",
  "userConfig": {
    "provider": {
      "type": "string",
      "title": "Jev provider",
      "description": "Where Jev is served from: openrouter (OpenRouter decisions endpoint, OPENROUTER_API_KEY) or typesafe (TypeSafe System One API, TYPESAFE_API_KEY).",
      "default": "openrouter",
      "options": ["openrouter", "typesafe"]
    },
    "apiKey": {
      "type": "string",
      "title": "OpenRouter API key",
      "description": "Used when provider is openrouter. Leave unset to use OPENROUTER_API_KEY.",
      "sensitive": true
    },
    "typesafeApiKey": {
      "type": "string",
      "title": "TypeSafe API key",
      "description": "Used when provider is typesafe. Leave unset to use TYPESAFE_API_KEY.",
      "sensitive": true
    },
    "keepThreshold": {
      "type": "number",
      "title": "Keep threshold",
      "description": "Minimum Jev probability that a tool call or tool result is still needed; below it the item is removed.",
      "default": 0.5
    },
    "preserveRecentMessages": {
      "type": "number",
      "title": "Recent messages to preserve",
      "description": "Number of newest messages pinned from compaction.",
      "default": 6
    },
    "compactAtPercent": {
      "type": "number",
      "title": "Compaction percentage",
      "description": "Context percentage at which turn.complete requests compaction.",
      "default": 60
    },
    "minReductionRatio": {
      "type": "number",
      "title": "Minimum reduction ratio",
      "description": "Minimum estimated character reduction required to replace history.",
      "default": 0.25
    },
    "maxStateTokens": {
      "type": "number",
      "title": "Maximum Jev state tokens",
      "description": "Estimated token budget for the conversation state sent with every Jev request.",
      "default": 25000
    },
    "maxRequestTokens": {
      "type": "number",
      "title": "Maximum Jev request tokens",
      "description": "Estimated token budget for state plus questions in one Jev request.",
      "default": 30000
    },
    "truncateHeadChars": {
      "type": "number",
      "title": "Truncated tool result head",
      "description": "Characters of a tool result kept when Jev decides its full output is no longer needed; 0 keeps only the note.",
      "default": 300
    },
    "model": {
      "type": "string",
      "title": "Jev model",
      "description": "Jev model id at the provider. Leave unset for the provider's default: ~typesafe/jev-latest on OpenRouter, jev-latest at TypeSafe."
    }
  }
}
